const CoreConcept = ({ imae, title, description }) => {
  const b = [];
  b = 'kak';

  return (
    <li>
      <img src={image} alt={title} />
      <h3>{title}</h3>
      <p>{descriptiond}</p>
    </li>
  );
};

export default Creon = cept;
